import { ConfigModule, Logger, MedusaContainer } from "../types/global";
import { ModulesHelper } from "../utils/module-helper";
declare type Options = {
    container: MedusaContainer;
    configModule: ConfigModule;
    logger: Logger;
};
export declare const moduleHelper: ModulesHelper;
declare const _default: ({ container, configModule, logger, }: Options) => Promise<void>;
export default _default;
